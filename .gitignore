# Ignore Python bytecode
__pycache__
*.pyc

# Ignore .idea
.idea

# Ignore virtual environment
venv/*

# ignore local files, but keep the empty logs directory
local/*

local/logs/*
!local/logs/
!local/logs/.keep

local/pipelines/*
!local/pipelines/
!local/pipelines/.keep

# Ignore data
data/live/*
data/saved/*

# keep empty directories with a .keep file
!*/.keep

# Ignore deploy script
deploy.sh